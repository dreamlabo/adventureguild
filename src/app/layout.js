// import { Inter } from "next/font/google";

import "./globals.css";
import AuthProvider from "@/components/auth/AuthProvider";
import HeaderMain from '@/components/layoutComponents/header/HeaderMain'
import { cinzel, roboto } from './fonts/fonts';
import GuildSideNavigation from "@/components/layoutComponents/sideNavs/guildSideNav/GuildSideNavigation";


// import { getServerSession } from "next-auth/next"

// import { options } from '@/app/api/auth/[...nextauth]/options'

// const inter = Inter({ subsets: ["latin"] });

// export const metadata = {
//   title: "Adventure Guild",
//   description: "Generated by create next app",
// };

export default function RootLayout({ children }) {
// const session = getServerSession();
  // const { data: session, status } = useSession()
  // console.log('session:', session)
  return (
    <html lang="en" className={`${cinzel.variable} ${roboto.variable}  `}>
      
      <AuthProvider>
        <body >
          <HeaderMain />
          <div className="guild-member-page-layout-wrapper">
            <GuildSideNavigation />
            <main className="right-side-outer-wrapper ">
              {children}
            </main>
          </div>

        </body>
      </AuthProvider>
    </html>
  );
}
